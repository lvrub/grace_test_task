{
  "compilerOptions": {
  "baseUrl": ".",
        "paths": {
          "@config" :["playwright.config"],
          "@fix" :["./fixture/pages.ts"]

        }
      },
      "strict": true, 
      // "target": "ES2020",           // Set the JavaScript version for output files
      // "module": "CommonJS",  
      // "outDir": "./dist", 
    }